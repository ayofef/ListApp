import styled from 'styled-components';
import { customTypographyBase } from './customTypographyBase';

export const H2 = styled.p`
  font-size: 36px;
  font-weight: 700;
  line-height: 36px;
  ${customTypographyBase};
`;
