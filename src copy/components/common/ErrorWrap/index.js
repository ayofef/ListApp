import styled from 'styled-components';
import { FlexContainer } from '../../atoms/flex/FlexContainer';

const ErrorWrap = styled(FlexContainer)`
  flex-direction: column;
`;

export default ErrorWrap;
