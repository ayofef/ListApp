import React from 'react';
import CustomProgressComponent from './CustomProgress';

export const CustomProgress = (args) => <CustomProgressComponent {...args} />;

export default {
  title: 'Atoms/CustomProgress',
  component: CustomProgressComponent,
};
